# The method (remove) and method (discard) take an argument and remove it from the collection
# متد (remove) و متد (discard) یک آرگومان میگیرند و ان را از مجموعه حذف میکنند

myset={6,'h',True,589,'hello',8,"fg",84,'A'}
myset.remove(True)
myset.discard(589)
print(myset)

#The clear method removes all elements from the set
# متد (clear) همه عناصر را از مجموعه حذف میکند

